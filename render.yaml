services:
  - type: static
    name: global-admin-estate-dashboard
    buildCommand: pnpm run build
    packageManager: pnpm
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
