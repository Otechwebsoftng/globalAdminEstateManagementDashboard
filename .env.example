# Live backend. The client appends /api/v1 itself, so omit it here.
VITE_API_BASE_URL=https://estatemanagementapiserver.onrender.com

# Mock seam for screens with no backend yet (see src/services/mockFlags.ts).
VITE_USE_MOCKS=true
